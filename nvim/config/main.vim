execute 'source' fnamemodify(expand('<sfile>'), ':h').'/插件.vim'

filetype plugin indent on

execute 'source' fnamemodify(expand('<sfile>'), ':h').'/拓展功能.vim'

execute 'source' fnamemodify(expand('<sfile>'), ':h').'/autocmd.vim'

execute 'source' fnamemodify(expand('<sfile>'), ':h').'/map.vim'

execute 'source' fnamemodify(expand('<sfile>'), ':h').'/其他配置项.vim'
